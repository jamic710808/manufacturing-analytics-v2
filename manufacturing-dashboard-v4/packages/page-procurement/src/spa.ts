/**
 * page-procurement - Single-SPA Entry Point (Build Entry)
 * 採購分析頁面
 *
 * 此檔案作為 vite-plugin-single-spa 的構建入口
 * 開發模式使用 src/index.tsx
 */
export { bootstrap, mount, unmount, default } from './index';
